RewriteEngine On

# Redirigir todo al directorio public solo si no está ya en public
RewriteCond %{REQUEST_URI} !^/Proyecto-Ingenieria-REGISTRO/public/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/$1 [L]
